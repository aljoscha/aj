//! Renders the top header: thread id, sandbox-warning banner
//! (until dismissed), and the `Resuming conversation …` notice on
//! resume.
//!
//! Filled in by the "Interactive TUI: layout slots, event pump,
//! components" step in Phase 1 of `docs/aj-next-plan.md`.
