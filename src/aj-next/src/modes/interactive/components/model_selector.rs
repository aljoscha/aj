//! Renders the model-selector overlay (`/model`). Consumes the
//! shared model catalog from `aj-models`; honours the
//! `disabled_tools` filter from `aj-conf`.
//!
//! Filled in by the "Selectors and theming" step in Phase 1 of
//! `docs/aj-next-plan.md`.
