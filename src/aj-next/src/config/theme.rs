//! Theme — colour palette + style definitions for the interactive
//! mode.
//!
//! Loads a default palette from `aj-conf` and exposes a small set
//! of accessor methods (`primary()`, `accent()`, `dim()`, …)
//! consumed by the per-component renderers under
//! [`crate::modes::interactive::components`].
//!
//! Filled in by the "Selectors and theming" step in Phase 1.
