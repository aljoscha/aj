//! Proc-macro support for `vaxis`.
//!
//! Will host `#[derive(TableRow)]`, the replacement for Zig's comptime struct
//! reflection in the `Table` widget: it derives the column headers from field
//! names and the cell formatting from field types. No macro yet.
