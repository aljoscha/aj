- ~~refactor cli related structs in the aj crate: we should factor out the common code between cli.rs and sub_agent_cli.rs and re-use that between the two implementations. We also must change the sub-agent cli so that it _does_ ask for permissions for tools instead of returning a blanket false~~
- we need to change how UserOutput is returned from tool calls. Currently, we return it from tools and then record it in the conversation and display it. We want to change this to instead give the tool access to an AjUi in the call. The tool implementations directly call methods on that for displaying output. We then build a special wrapping AjUi implementation that wraps the actual AjUi that the Agent is given and also records all the given UserOutput, so that at the end of a turn we can append it to the conversation
