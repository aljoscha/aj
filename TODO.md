## Persistent/Restartable Threads

- need to refactor how agent interacts with UI, it must return information that
  we can both write down and display in UI
- need to be able to write down conversation and output, so that we can
  re-create

## "Rich" UI, maybe using Ratatui
